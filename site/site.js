const prompt=document.querySelector('.spot-prompt');const commands=prompt?.dataset.commands?.split('|')||[];let index=0;if(prompt&&commands.length&&!matchMedia('(prefers-reduced-motion: reduce)').matches){setInterval(()=>{index=(index+1)%commands.length;prompt.animate([{opacity:0,transform:'translateY(5px)'},{opacity:1,transform:'translateY(0)'}],{duration:280});prompt.textContent=commands[index]},2600)}const light=document.querySelector('.cursor-light');addEventListener('pointermove',event=>{if(!light)return;light.style.left=`${event.clientX}px`;light.style.top=`${event.clientY}px`},{passive:true});
